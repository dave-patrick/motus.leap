
# Tube Manager

A playlist-backed saved-video manager with Google/Youtube-style task flows.

## Repo layout
- `tube_manager/` — app source package
- `tests/` — validation and harness coverage
- `docs/` — design notes, runbooks, and decision log

## Getting started
See `docs/` for architecture notes and `tube_manager/` for implementation tasks.
